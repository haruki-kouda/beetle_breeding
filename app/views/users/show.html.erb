
   <%= "#{@user.nickname}の BREED 情報"%>
  
    
      <%= "BREEDER" %> 
      <p >名前
       <%= @user.nickname %></p>
      <p>ブリード歴
      <%= "#{@user.breeding_history}年" %></p>
      <p>プロフィール</p>
       <%= @user.profile %>
      <p>
     
    
  
   
      <%= "投稿内容  一覧表示" %> 
   
  
    <ul class='item-lists'>
        <% @breeds.each do |breed| %>
       <li class="breed-show">
        <p>
        <%= breed.name %>
        </p>
        <p>
        <%= breed.date %>
        </P>
        <div class="user__image">
        <%= link_to image_tag(breed.image,), breed_path(breed.id) %>
        </div>
        <% end %>
      </li>
    </ul>
  